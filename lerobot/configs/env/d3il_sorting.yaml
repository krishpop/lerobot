# @package _global_

fps: 33

env:
  name: sorting
  task: sorting-v0
  image_size: 96
  state_dim: 14
  action_dim: 2
  fps: ${fps}
  gym:
    max_steps_per_episode: 1000
    if_vision: true
    render: false
    self_start: true